= render 'layouts/header'
.o_white
  .o_top-image
    = image_tag("https://www-mercari-jp.akamaized.net/assets/img/featured/camp/index/index_keyvisual.png?891577966", height: "400px")
  .o_pickup-container
    %h2.o_pickup-head
      ピックアップカテゴリ
      .o_topitems-boxes
        .o_topitems-box
          = link_to "#" do
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
  .o_pickup-container
    %h2.o_pickup-head
      ピックアップブランド
      .o_topitems-boxes
        .o_topitems-box
          = link_to "#" do          
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
            = render partial: 'item', locals: { item: @items }
= render 'layouts/footer'
= render 'layouts/sell-btn'