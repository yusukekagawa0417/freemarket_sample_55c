= render 'layouts/header'
- breadcrumb :confirmation, @user
= render "users/top_column"
.s_main
  .s_wrapper
    .s_left-side
      = render "users/mypage_sidebar"
    .s_right-side
      = render partial: 'confirmation', locals: {user: @user}

= render 'layouts/footer'
= render 'layouts/sell-btn'